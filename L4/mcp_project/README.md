```shell
uv init --python 3.11
uv sync
source .venv/bin/activate
```
### to install python packages in the uv python env
```sh
uv add mcp arxiv
```
### to run the app
```sh
uv run search_server.py
```
### to run the inspector:
```sh
 npx @modelcontextprotocol/inspector
```
